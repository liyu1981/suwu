# Copy to .env and adjust as needed. Values are loaded at startup; variables
# already present in the shell environment take precedence over this file.

# Bind address (default 127.0.0.1). Use 0.0.0.0 to expose on all interfaces.
HOST=127.0.0.1

# HTTP port (default 8080, or 8000 in dev mode).
PORT=8080

# Port of the Go demo server for `pnpm dev:web` (Vite) to proxy /api and /ws.
# Leave unset to use the Go dev default (8000), or set to match PORT above.
#DEMO_PORT=8080

# Extra browser-visible hostnames allowed through auth (comma-separated).
# Required when binding to a non-loopback address such as 0.0.0.0.
#GHOSTTY_ALLOWED_HOSTS=192.168.0.221

# TLS (opt-in). Set both to enable https:// — browsers only expose clipboard
# APIs (terminal paste) on secure contexts, so non-localhost HTTP access
# cannot paste into the terminal. Generate a dev pair with mkcert:
#   mkcert -cert-file var/tls-cert.pem -key-file var/tls-key.pem 192.168.0.221
# and trust the mkcert root CA (mkcert -CAROOT/rootCA.pem) on client devices.
#TLS_CERT_FILE=var/tls-cert.pem
#TLS_KEY_FILE=var/tls-key.pem